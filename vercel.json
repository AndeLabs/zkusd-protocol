{
  "buildCommand": "pnpm turbo run build --filter=@zkusd/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.1 --activate && pnpm install",
  "framework": "nextjs",
  "devCommand": "pnpm turbo run dev --filter=@zkusd/web"
}
